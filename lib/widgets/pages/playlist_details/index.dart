import 'package:flutter/widgets.dart';

class PlaylistDetailsPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Playlist Details"));
  }
}