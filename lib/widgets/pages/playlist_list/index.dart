import 'package:flutter/material.dart';

class PlaylistListPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Playlist List"));
  }
}